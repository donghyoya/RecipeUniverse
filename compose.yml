services:
  mysql:
    image: 'mariadb:latest'
    environment:
      - 'MYSQL_DATABASE=dev'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306:3306'

